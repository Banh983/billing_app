import 'package:flutter/material.dart';

class BillingEmptyState extends StatelessWidget {
  const BillingEmptyState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Không có kỳ cước"));
  }
}
